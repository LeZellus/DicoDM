{% extends 'base.html.twig' %}

{% block title %}Ajouter un mot{% endblock %}

{% block body %}
    <div class="flex min-h-full items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="w-full max-w-md space-y-8">
            <div>
                <div class="flex items-center justify-center">
                    <p class="text-4xl font-bold uppercase"><span class="text-pink-400">D</span>ico <span class="text-green-400">D</span>es <span class="text-blue-400">M</span>ots</p>
                    <img class="ml-4 pb-2 h-12 w-auto" src="{{ asset('uploads/open-book.png') }}" alt="Your Company">
                </div>
                <h2 class="mt-6 text-center text-3xl font-bold tracking-tight">Ajouter une définition</h2>
            </div>

            {{ form_start(form, {'attr':  {'class': 'mt-8 space-y-6'}}) }}
                {{ form_errors(form) }}

                <div>
                    {{ form_widget(form.name) }}
                    {{ form_widget(form.definition) }}
                </div>
                {{ form_widget(form.category) }}

                {{ form_widget(form.submit) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
