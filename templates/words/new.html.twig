{% extends 'base.html.twig' %}

{% block title %}Ajouter un mot | {{ parent() }}{% endblock %}

{% block body %}
    <div class="flex flex-col min-h-full items-center justify-center px-4 sm:px-6 lg:px-8 pb-8">
        <div class="w-full max-w-md space-y-8">
            <div>
                <h2 class="mt-6 text-center text-3xl font-bold tracking-tight">Ajoutez un mot ou un prénom</h2>
                <p class="mt-2 text-center text-base">
                    Pas encore inscrit ?
                    <a href="{{ path('app_register') }}" class="font-medium text-green-400 hover:text-green-200">Inscription</a>
                </p>
            </div>
            {{ form_start(form, {'attr':  {'class': 'mt-8 space-y-6'}}) }}

                <div class="text-red-400 space-y-2 text-center">
                    {{ form_errors(form.name) }}
                    {{ form_errors(form.definition) }}
                </div>
                <div>
                    {{ form_widget(form.name) }}
                    {{ form_widget(form.exemple) }}
                    {{ form_widget(form.definition) }}
                </div>
                {{ form_widget(form.category) }}

                {{ form_widget(form.submit) }}
            {{ form_end(form) }}
        </div>

        <div class="grid gap-4 mt-6 w-full max-w-md text-justify">
            <h2 class="mt-6 text-center text-3xl font-bold tracking-tight">Comment ça marche ?</h2>
            <p>Il te suffit simplement de remplir les champs, je te donne ici quelques conseils :</p>
            <ul class="pl-6 list-disc grid gap-2">
                <li>Dans le champ Nom/Prénom, va à l'essentiel ! Un ou deux mots suffit 😁</li>
                <li>Pour l'exemple, écris une petite phrase qui met ton mot/expression en situation !</li>
                <li>Pour finir, tu peux indiquer une définition qui explique ton mot ou ton expression</li>
                <li>N'hésite pas à être original et fun ! 🥳</li>
            </ul>
            <p>Bon courage !</p>
        </div>
    </div>
{% endblock %}
